<% if current_user && groups?%> 
  <div class="row text-center">
    <div class="<%=fixed(3)%>"></div>
    <div class="<%=fixed(6)%>">
      <h1> Almost there: time to find the stuff you need. </h1>
      <%= render partial: 'group_name_list', locals: {group_members: @group_members, group: @group}%>
      <br> <br>  
    </div>
    <div class="<%=fixed(3)%>">
  </div>

<% end %>

